#insertion sort
a=[7,5,16,2,30]
n=len(a)
for i in range(1,n):
    key=a[i]
    j=i-1
    while j>=0 and a[j]>key:
        a[j+1]=a[j]
        j-=1
    a[j+1]=key
    
print('sorted array : ',a)
